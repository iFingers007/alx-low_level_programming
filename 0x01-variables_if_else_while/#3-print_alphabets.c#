#include <stdio.h>

/**
 *main- Entry point
 *
 *Return: Always 0 at success
*/

int main(void)
{
	char a;

	a = 'a';
	while (a <= 'z')
	{
		putchar(a);
		a++;
	}
	char b = 'A';

	while(b <= 'Z')
	{{[
		putchar(b);
	
	putchar('\n');

	return (0);
}
