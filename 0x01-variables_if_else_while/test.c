#include <stdio.h>

/**
 *main- Entry point
 *
 *Return: Always 0 at success
*/

int main(void)
{
	int i,j;

	for (i = 0; i <= 15; i++)
	{
		for (j = 0, j <= 9; j++)
			putchar(
		putchar(48 + i);
	for (j = 'a'; j <= 'f'; j++)
		putchar(j);
	putchar('\n');

	return (0);
}
