#include "main.h"
/**
 * _isdigit - For printing digit 0 to 9
 * *@c: character for argument
 *
 * Return: 0
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
	return (1);
}
else
{
	return (0);
}
}
