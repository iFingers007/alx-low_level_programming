#include "main.h"
/**
 * _abs - For printing sign absolute
 * *@n: int for the argument of function
 *
 * Return: 0
 */

int _abs(int n);
{
	if (n > 0 || n == 0)
	{
		return (n);
	}
		else
		{
			return (n * -1);
		}

}
