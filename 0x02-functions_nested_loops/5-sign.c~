#include "main.h"
/**
 * print_sign - For printing sign oa number
 * *@c: character for argument
 *
 * Return: 0
 */

int print_sign(int n)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
{
	return (1);
}
else
{
	return (0);
}
}
